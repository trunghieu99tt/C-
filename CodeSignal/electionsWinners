int electionsWinners(std::vector<int> a, int k) {
    int i,counter=1;
    sort(a.begin(),a.end(),greater<int>());
    if(a[0]==a[1] && k==0) return 0;
    for (i=1;i<a.size();i++)
        if(a[i]+k>a[0]) counter++;
    return counter;
}
