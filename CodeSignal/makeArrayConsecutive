std::vector<int> makeArrayConsecutive(std::vector<int> a) {
    int n=a.size(),i;
    vector<int> result;
    sort(a.begin(),a.end());
    for (i=1;i<a.size();i++)
    {
        if(a[i]-a[i-1]>1)
            for (int j=a[i-1]+1;j<a[i];j++) result.push_back(j);
    }
    return result;
}
